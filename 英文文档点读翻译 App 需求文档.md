# 英文文档点读翻译 App 需求规格说明书

> 版本：V1.0 | 状态：草稿 | 视觉与交互设计已完成

---

# 一、引言

## 1.1 编写目的

本文档旨在明确「英文文档点读翻译 App」的功能需求与非功能需求，作为后续开发、测试与验收的统一依据。

## 1.2 产品定位

一款面向英语学习者的移动端阅读辅助工具，核心能力为「上传文档 → 智能识别 → 点词翻译 + 短语提取」。用户在手机端打开任意英文文档（PDF / Word / 图片 / TXT），点击任意单词即可即时查看释义，同时自动识别该单词所在句子中的固定搭配和短语。

## 1.3 目标用户

| 用户群体 | 典型场景 | 核心诉求 |
|---|---|---|
| 大学生/研究生 | 阅读英文论文、教材 | 快速理解专业词汇，积累学术短语 |
| 职场人士 | 阅读英文邮件、行业报告 | 高效读懂商务文档，不打断工作流 |
| 英语自学者 | 阅读英文原版小说、文章 | 在阅读中自然背单词、学搭配 |
| 备考人群 | 做真题、读外刊 | 精准掌握高频词汇和考点短语 |

## 1.4 参考资料

- 《英文文档点读翻译 App 设计方案》
- chapter3.xml / chapter7_8.xml（示例文档数据）

---

# 二、总体描述

## 2.1 产品视角

产品从用户上传英文文档开始，完成解析后进入阅读界面，用户在阅读过程中点击单词即可获取即时翻译和短语信息，查过的单词自动收录至生词本，形成「阅读 → 查询 → 积累」的学习闭环。

## 2.2 用户核心流程

```
上传文档 → 文档解析 → 打开阅读 → 点击单词 → 查看翻译/短语 → 自动收录生词本
```

## 2.3 运行环境

- 移动端：iOS 14+ / Android 8.0+
- 网络：离线可用（核心功能），在线增强（扩展翻译、云同步）
- 存储：本地数据库（SQLite）+ 文件沙盒

## 2.4 设计约束

- 核心操作不超过 2 步（打开文件 → 点单词）
- 点词后 < 100ms 内出现选中态且翻译卡片开始响应（本地）；卡片转场完成时间 ≤ 250ms
- OCR 识别准确率 > 95%
- 1000 页 PDF 解析时间 < 30s
- 数据默认本地处理，敏感文档不上传云端

---

# 三、功能需求

## 3.1 文档管理模块 (P0)

### FR-DOC-001 文档导入

| 编号 | FR-DOC-001 |
|---|---|
| **描述** | 用户可通过多种方式将英文文档导入 App |
| **优先级** | P0 |
| **详细需求** | |
| | 1. 支持调用系统文件选择器，从本地存储选择文件 |
| | 2. 支持从微信、QQ 等第三方 App 通过「分享/用其他应用打开」导入 |
| | 3. 支持的文件格式：PDF（文本型）、Word（.docx）、TXT |
| | 4. 导入时显示进度提示，完成后跳转至文档列表页 |
| **验收标准** | |
| | - 能成功导入 PDF、Docx、TXT 格式文件 |
| | - 导入进度实时可见 |
| | - 导入失败时有明确错误提示 |

### FR-DOC-002 文档解析

| 编号 | FR-DOC-002 |
|---|---|
| **描述** | 系统对导入的文档进行结构化解析 |
| **优先级** | P0 |
| **详细需求** | |
| | 1. 根据文件后缀和魔数自动识别文件真实格式 |
| | 2. 提取纯文本及段落结构信息（段落样式、标题层级） |
| | 3. 对全文进行分句、分词处理，建立「段落 → 句子 → 单词」三级索引 |
| | 4. 对全文进行词性标注和短语预识别，结果缓存至本地数据库 |
| | 5. 解析状态在文档列表中可视化展示（解析中/已完成/失败） |
| **验收标准** | |
| | - PDF 文本型文档能正确提取段落和文本 |
| | - Word 文档的正文内容完整提取，不包含格式标记 |
| | - TXT 文档自动识别编码（UTF-8/GBK 等），中文不出现乱码 |
| | - 分句准确率 > 95%，分词正确率 > 98% |

### FR-DOC-003 文档列表管理

| 编号 | FR-DOC-003 |
|---|---|
| **描述** | 用户可查看和管理已导入的文档 |
| **优先级** | P0 |
| **详细需求** | |
| | 1. 文档以卡片列表形式展示，每张卡片显示：标题、格式图标、字数/页数、上次阅读时间、阅读进度百分比 |
| | 2. 支持按标题搜索文档 |
| | 3. 支持按导入时间/标题排序 |
| | 4. 左滑露出操作区，点击「删除」后显示二次确认弹窗；不支持滑到底直接删除 |
| | 5. 支持分类/标签管理（P1） |
| **验收标准** | |
| | - 文档列表正确展示所有已导入文档 |
| | - 搜索功能能按标题模糊匹配 |
| | - 删除操作有二次确认，删除后列表实时更新 |

---

## 3.2 阅读与点译模块 (P0)

### FR-READ-001 文档阅读渲染

| 编号 | FR-READ-001 |
|---|---|
| **描述** | 在阅读界面正确渲染文档内容 |
| **优先级** | P0 |
| **详细需求** | |
| | 1. 全文以可滚动方式展示，每个单词为独立可点击的文本元素 |
| | 2. 保留原文档的段落结构，标题层级以不同字号/粗细区分 |
| | 3. 支持字体大小调节（逻辑字号 12-24，iOS 使用 pt、Android 使用 sp，默认 16） |
| | 4. 支持行间距调节（1.4-2.0，默认 1.6） |
| | 5. 支持三种阅读主题切换：日间（白底黑字）、夜间（深灰底浅灰字）、护眼（米黄底深棕字） |
| | 6. 底部细进度条显示当前阅读进度，支持拖动跳转；翻译卡片打开期间隐藏并禁用拖动，卡片完全收起后恢复 |
| **验收标准** | |
| | - 文本内容与原文档一致，不丢字、不错位 |
| | - 字体大小和主题切换即时生效 |
| | - 进度条位置准确反映阅读进度 |
| | - 翻译卡片与阅读进度条不会同时响应底部手势，卡片收起后进度条正常恢复 |

### FR-READ-002 点词翻译

| 编号 | FR-READ-002 |
|---|---|
| **描述** | 用户点击单词后即时显示翻译信息 |
| **优先级** | P0 |
| **详细需求** | |
| | 1. 单击任意英文单词，该单词显示 2pt/dp 的当前主题强调色下划线，并填充当前主题强调色 12% 透明度的底色 |
| | 2. 底部翻译卡片从下向上滑出，动画时长 200ms |
| | 3. 点击单词所在句子自动高亮显示，帮助用户理解上下文 |
| | 4. 点击正文其他区域或下滑卡片，翻译面板收起 |
| | 5. 连续点击不同单词时，卡片内容平滑切换，不重新执行弹出动画 |
| | 6. 快速滑动时不触发点词，仅滚动不被识别为点击操作 |
| **验收标准** | |
| | - 点击单词后 < 100ms 内出现单词选中态且翻译卡片开始响应，转场在 250ms 内完成 |
| | - 点击的单词准确高亮，句子也准确高亮 |
| | - 滑动时不误触单词点击 |
| | - 卡片弹出/收起动画流畅无卡顿 |

### FR-READ-003 翻译卡片内容

| 编号 | FR-READ-003 |
|---|---|
| **描述** | 翻译卡片展示单词的详细信息 |
| **优先级** | P0 |
| **详细需求** | |
| | 1. **必显内容**：单词原文（大号字体）、音标、发音按钮、词性标签、核心释义 |
| | 2. **短语区**：标题「本句短语」，列出当前句子中包含该单词的所有短语 |
| | 3. 每条短语显示：短语原文、类型标签（动词短语/介词短语/习语/固定搭配）、中文释义 |
| | 4. 每条短语旁有「+」按钮，点击可加入短语本 |
| | 5. 点击某条短语时，若正文中的对应片段不在卡片上方可视区域内，先以最小距离将其定位到可视区域，再执行一次柔和的背景高亮淡出 |
| | 6. 卡片采用自适应两档高度：紧凑档目标为可用安全区域的 40%，可在 35%-50% 之间调整以容纳核心内容；上滑进入展开档，目标为 65%，必要时最高可达 90%，内部内容可滚动 |
| **验收标准** | |
| | - 卡片内容信息准确，释义与单词实际含义匹配 |
| | - 短语列表正确显示当前句子中的相关短语 |
| | - 「+」按钮点击后短语成功加入短语本并有反馈 |
| | - 点击短语后对应正文片段保持可见，并完成一次清晰但不打断阅读的高亮反馈 |

### FR-READ-004 短语识别

| 编号 | FR-READ-004 |
|---|---|
| **描述** | 自动识别单词在当前句子中构成的短语 |
| **优先级** | P0 |
| **详细需求** | |
| | 1. 识别范围包括：动词短语（look up, give up）、介词短语（in addition to）、固定搭配（take advantage of）、习语（piece of cake） |
| | 2. 短语识别采用「本地短语库 + 规则匹配」方式，离线可用 |
| | 3. 对识别结果标注置信度，低置信度的短语标注「可能」或不展示 |
| | 4. 常用短语识别准确率 > 85%，误判率 < 10% |
| **验收标准** | |
| | - 能正确识别常见短语（如 look up、give up、in addition to 等） |
| | - 不能将非短语组合误判为短语 |
| | - 离线环境下短语识别正常工作 |

---

## 3.3 词典与翻译引擎 (P0)

### FR-DICT-001 本地词典

| 编号 | FR-DICT-001 |
|---|---|
| **描述** | 内置本地英汉词典，支持离线查询 |
| **优先级** | P0 |
| **详细需求** | |
| | 1. 内置常用英汉词典（约 5 万词条） |
| | 2. 查询响应时间 < 10ms |
| | 3. 返回内容包含：基础释义、音标、词性标注 |
| | 4. 支持词形还原查询（如 looked → look、studies → study） |
| **验收标准** | |
| | - 断网状态下能正常查询常见单词 |
| | - 查询结果准确，释义可信 |
| | - 查询延迟不可感知（本地查询 < 10ms，点击后 < 100ms 内出现首帧 UI 反馈） |

### FR-DICT-002 在线翻译增强

| 编号 | FR-DICT-002 |
|---|---|
| **描述** | 有网络时补充在线翻译内容 |
| **优先级** | P1 |
| **详细需求** | |
| | 1. 网络可用时，调用在线翻译 API 补充更多释义、例句、同义词、词形变化 |
| | 2. 离线时仅展示本地词典结果，不报错 |
| | 3. 在线查询结果缓存至本地，相同单词 24 小时内不重复请求 |
| | 4. 已发起的在线请求若超时或失败，保留本地结果，并在在线增强区域显示非阻塞弱提示「在线增强暂不可用，已显示本地释义」和「重试」操作 |
| | 5. 用户主动开启隐私模式、关闭在线增强或请求前已确定设备离线时，不显示失败提示 |
| **验收标准** | |
| | - 有网络时卡片展示内容比离线更丰富 |
| | - 无网络时不报错，正常使用本地词典 |
| | - 缓存机制有效减少 API 调用次数 |
| | - 在线请求失败时本地内容保持可读、可操作，点击「重试」可重新发起请求 |
| | - 隐私模式、主动离线和未启用在线增强时不出现误导性的网络错误提示 |

---

## 3.4 学习沉淀模块 (P1)

### FR-LEARN-001 生词本

| 编号 | FR-LEARN-001 |
|---|---|
| **描述** | 自动收录查询过的单词，形成个人生词本 |
| **优先级** | P1 |
| **详细需求** | |
| | 1. 用户点击翻译的单词默认自动加入生词本（可在设置中关闭） |
| | 2. 支持手动方式添加：长按单词选择「加入生词本」，或手动输入单词添加 |
| | 3. 生词本列表展示：单词、音标、简短释义、熟练度标记、查询次数 |
| | 4. 熟练度分三档：认识/模糊/陌生 |
| | 5. 支持按熟练度筛选、按时间/字母/频率排序 |
| | 6. 支持关键字搜索 |
| | 7. 左滑露出操作区，点击「删除」后删除单条记录；不支持滑到底直接删除 |
| | 8. MFV 版本支持按来源文档分组查看 |
| **验收标准** | |
| | - 点词翻译后单词自动出现在生词本 |
| | - 熟练度标记可切换且状态持久化 |
| | - 筛选、排序、搜索功能均正常 |

### FR-LEARN-002 短语本

| 编号 | FR-LEARN-002 |
|---|---|
| **描述** | 用户可主动收录短语，形成个人短语本 |
| **优先级** | P1 |
| **详细需求** | |
| | 1. 通过翻译卡片中短语旁的「+」按钮收录 |
| | 2. 收录时自动保存该短语出现的原句（语境保存） |
| | 3. 短语按类型分类展示：动词短语/介词短语/习语/商务搭配 |
| | 4. 支持搜索和删除 |
| **验收标准** | |
| | - 收录的短语正确保存到短语本 |
| | - 原句信息一同保存，可回看来源 |
| | - 分类筛选功能正常 |

### FR-LEARN-003 学习统计

| 编号 | FR-LEARN-003 |
|---|---|
| **描述** | 展示用户的学习数据概况 |
| **优先级** | P1 |
| **详细需求** | |
| | 1. 展示累计查询单词数、收录生词数、收录短语数 |
| | 2. 展示本周/本月查询趋势（简单图表） |
| | 3. 展示阅读文档数和阅读时长 |
| **验收标准** | |
| | - 数据统计与实际行为一致 |
| | - 图表展示直观可读 |

### FR-LEARN-004 导出功能

| 编号 | FR-LEARN-004 |
|---|---|
| **描述** | 支持将生词本导出为外部格式 |
| **优先级** | P1 |
| **详细需求** | |
| | 1. 支持导出为 CSV 格式 |
| | 2. 支持导出为 Anki 导入格式（制卡包） |
| | 3. 可选择导出范围（全部/按熟练度/按文档） |
| **验收标准** | |
| | - 导出文件内容完整、格式正确 |
| | - Anki 格式可被 Anki App 正常导入 |

---

## 3.5 个人中心 (P1)

### FR-USER-001 设置

| 编号 | FR-USER-001 |
|---|---|
| **描述** | 提供应用的全局设置 |
| **优先级** | P1 |
| **详细需求** | |
| | 1. 阅读设置：默认字体大小、默认主题、默认行间距 |
| | 2. 学习设置：是否自动收录生词本、复习提醒开关 |
| | 3. 缓存管理：查看缓存大小、一键清理 |
| | 4. 关于：版本号、用户协议、隐私政策、反馈入口 |
| **验收标准** | |
| | - 各项设置修改后即时生效 |
| | - 缓存清理后空间确实释放 |

---

## 3.6 复习功能 (P1)

### FR-REVIEW-001 艾宾浩斯复习

| 编号 | FR-REVIEW-001 |
|---|---|
| **描述** | 基于遗忘曲线提醒用户复习生词 |
| **优先级** | P1 |
| **详细需求** | |
| | 1. 基于艾宾浩斯遗忘曲线算法，在关键遗忘节点推送复习提醒 |
| | 2. 复习以卡片形式呈现：正面显示英文单词，点击翻转显示中文释义 |
| | 3. 复习反馈三档：认识/模糊/不认识，影响下次复习间隔 |
| | 4. 支持听写模式：播放单词发音，用户手动拼写输入 |
| **验收标准** | |
| | - 提醒在合理时间节点触发 |
| | - 反馈后的复习间隔符合遗忘曲线规律 |
| | - 发音准确，拼写校验正确 |

---

# 四、非功能需求

## 4.1 性能需求

| 编号 | 指标 | 要求 |
|---|---|---|
| NFR-PERF-001 | 点词响应时间 | 从点击到首帧 UI 反馈 < 100ms（本地），翻译卡片转场完成 ≤ 250ms |
| NFR-PERF-002 | 本地词典查询 | < 10ms |
| NFR-PERF-003 | 大文档解析 | 1000 页 PDF 解析 < 30s |
| NFR-PERF-004 | 应用冷启动 | < 2s |
| NFR-PERF-005 | 阅读滚动帧率 | ≥ 55 FPS |
| NFR-PERF-006 | 翻译卡片动画 | 60 FPS，无卡顿 |

## 4.2 可用性需求

| 编号 | 指标 | 要求 |
|---|---|---|
| NFR-USAB-001 | OCR 识别准确率 | > 95% |
| NFR-USAB-002 | 分句准确率 | > 95% |
| NFR-USAB-003 | 分词正确率 | > 98% |
| NFR-USAB-004 | 短语识别准确率 | 常用短语 > 85%，误判率 < 10% |
| NFR-USAB-005 | 界面操作步骤 | 核心操作 ≤ 2 步 |
| NFR-USAB-006 | 离线可用性 | 基础翻译和文档解析 100% 离线可用 |
| NFR-USAB-007 | 触控区域 | 交互控件最小触控区域为 iOS 44×44pt、Android 48×48dp |
| NFR-USAB-008 | 字体缩放 | 跟随系统字体缩放；核心释义和操作标签不得截断，必要时换行、增高或滚动 |
| NFR-USAB-009 | 减少动态效果 | 遵循系统设置，提供无共享元素飞入、弹跳、弹性过冲和重复闪烁的替代反馈 |
| NFR-USAB-010 | 辅助技术 | 图标按钮、状态变化和异步结果提供可被 VoiceOver / TalkBack 读取的标签或播报 |
| NFR-USAB-011 | 视觉对比度 | 普通文字与背景对比度 ≥ 4.5:1，大字号文字和关键控件状态 ≥ 3:1 |

## 4.3 安全与隐私需求

| 编号 | 要求 |
|---|---|
| NFR-SEC-001 | 文档默认在本地解析，不上传至服务器 |
| NFR-SEC-002 | 需明确告知用户哪些数据会上传（如在线翻译查询词条） |
| NFR-SEC-003 | 提供隐私模式选项，完全关闭网络请求 |
| NFR-SEC-004 | 本地数据库不存储用户明文密码或敏感凭证 |
| NFR-SEC-005 | 应用沙盒隔离，不访问外部存储的其他文件 |

## 4.4 兼容性需求

| 编号 | 要求 |
|---|---|
| NFR-COMP-001 | iOS 14.0 及以上 |
| NFR-COMP-002 | Android 8.0 (API 26) 及以上 |
| NFR-COMP-003 | 适配主流屏幕尺寸（手机 4.7" ~ 6.9"） |
| NFR-COMP-004 | 平板可运行但非优化目标（P2） |

## 4.5 可维护性需求

| 编号 | 要求 |
|---|---|
| NFR-MAIN-001 | 核心解析模块、翻译引擎模块需独立封装，可单独测试 |
| NFR-MAIN-002 | 支持动态下发本地词典和短语库更新包（不依赖发版） |
| NFR-MAIN-003 | 关键操作需埋点（点词、翻译查看、生词本收录），用于数据分析 |

---

# 五、数据需求

## 5.1 核心数据实体

| 实体 | 说明 |
|---|---|
| Document（文档） | 用户导入的英文文档元信息 |
| Paragraph（段落） | 文档的段落结构 |
| Sentence（句子） | 段落内的句子拆分 |
| Word（单词） | 句子内的单词索引 |
| Phrase（短语） | 句子中识别出的短语实例 |
| Vocabulary（生词） | 用户查询过的生词记录 |

## 5.2 数据关系

```
Document (1) ──→ (N) Paragraph ──→ (N) Sentence ──→ (N) Word
                                    Sentence ──→ (N) Phrase
Document (1) ──→ (N) Vocabulary（来源文档）
```

## 5.3 索引需求

- `words` 表：(document_id, lemma) 联合索引，快速查找某文档中某单词的所有出现
- `phrases` 表：sentence_id 索引，快速查询某句中的所有短语
- `vocabulary` 表：word 唯一索引，proficiency 索引

---

# 六、界面需求

## 6.1 文档列表页（首页）

- 顶部：搜索栏 + 「+」导入按钮
- 主体：文档卡片列表（标题、格式图标、字数/页数、上次阅读时间、阅读进度）
- 空状态：首次启动优先进入引导式导入；用户取消导入或删除最后一份文档后，显示极简文案「导入你的第一篇英文文档」和单一主按钮「导入文档」
- 底部导航栏：文档 | 生词本 | 短语本 | 我的

## 6.2 阅读页

- 顶部栏：返回 + 文档标题 + 更多菜单（字体/主题/目录/搜索）
- 正文区：全屏可滚动，每个单词可点击
- 底部翻译卡片：默认隐藏，点击单词后滑出；紧凑档目标高度 40%、展开档目标高度 65%，根据安全区域、内容和系统字体缩放自适应调整
- 底部进度条：细条进度指示；翻译卡片打开期间隐藏并禁用，卡片完全收起后恢复

## 6.3 翻译卡片

- 上半区：单词（大号）、音标、发音按钮、词性标签、核心释义
- 下半区：本句短语列表（短语原文 + 类型标签 + 释义 + 收录按钮）
- 展开区：更多释义、例句、同义词、词形变化

## 6.4 生词本页

- 顶部：筛选（全部/认识/模糊/陌生）+ 排序 + 搜索
- 列表：单词、音标、简短释义、熟练度标记、查询次数
- 交互：左滑露出操作区、点击删除并二次确认、点击进详情、长按多选
- 底部：开始复习按钮

## 6.5 视觉规范

- 色彩、字体、间距和组件样式统一引用第九章「视觉风格设计」，本节不另行维护重复色值
- 强调色：日间模式使用 `#3D7AED`，夜间模式使用 `#6B9FFF`，护眼模式使用 `#5D7EA8`
- 阅读正文：英文使用 Georgia / Charter，中文使用思源宋体 / Noto Serif CJK
- UI 界面：iOS 使用 SF Pro / 苹方，Android 使用 Roboto / 思源黑体
- 正文字号：逻辑字号 16-18（iOS 使用 pt、Android 使用 sp），行高 1.6-1.8；用户设置可覆盖默认值

---

# 七、版本规划

## 7.1 MVP（第 1-2 个月）— P0 需求

| 周次 | 里程碑 | 交付内容 |
|---|---|---|
| 1-2 | 项目搭建 + 文档解析 | 框架搭建、PDF/TXT 解析、文本结构化 |
| 3-4 | 阅读引擎 + 点译 | 文档渲染、单词点击、本地词典集成、翻译卡片 |
| 5-6 | 短语识别 + 生词本 | 短语识别、翻译卡片短语展示、生词本自动收录 |
| 7-8 | 打磨 + 测试 | UI 优化、性能优化、Bug 修复、内测发布 |

## 7.2 V1.0（第 3-4 个月）— P1 需求

- 文档增强：Word 支持、EPUB 支持、图片 OCR
- 翻译增强：在线翻译 API、发音、例句展示
- 学习功能：短语本、学习统计、导出 CSV/Anki
- 体验优化：搜索、目录导航、阅读设置完善

## 7.3 V2.0（第 5-6 个月）— P2 需求

- AI 增强：AI 语境翻译、单词用法讲解、易错点提示
- 复习系统：艾宾浩斯复习、卡片复习、听写模式
- 云同步：账号系统、文档与生词本云端同步
- 社区：笔记分享、热门书单、学习打卡

---

# 八、风险与应对

| 风险 | 等级 | 应对措施 |
|---|---|---|
| PDF 文本定位复杂度高 | 中 | 优先支持常见格式，复杂场景引导用户使用 OCR 模式 |
| 短语识别准确率不足 | 高 | 多重匹配策略 + 置信度标注，低置信度不展示或标注「可能」 |
| 大文档移动端性能瓶颈 | 中 | 分页加载、虚拟列表、后台解析、预加载机制 |
| OCR 准确率受图片质量影响 | 中 | 使用 ML Kit 等成熟方案，支持手动修正 |
| 用户留存困难 | 中 | 通过生词本、复习增加粘性；学习数据可视化激发成就感 |
| 翻译 API 成本增长 | 中 | 本地缓存 + 批量查询 + 免费额度利用 + 会员制分摊 |
| 用户隐私担忧 | 中 | 默认本地解析，明确告知上传数据范围，提供隐私模式 |
| 词典/短语库版权 | 低 | 使用开源词典数据，或与正规服务商合作 |

---

# 九、视觉风格设计

## 9.1 总体风格定位

**轻量极简**：整体风格参考 Apple Notes / Kindle，以最大程度让内容成为视觉主角为原则。大面积留白，抑制装饰元素，以间距、线条和灰度微差区分信息层级。

## 9.2 色板：石墨（Graphite）

整体配色极度克制，黑白灰为基调，单一强调色点缀功能节点。

### 9.2.1 日间模式

| 色值 | 用途 |
|---|---|
| `#1A1A1A` | 主色 — 标题、正文、图标、导航栏文字 |
| `#3D7AED` | 强调色 — 选中态、链接、操作按钮、进度指示、Tab 激活态 |
| `#FFFFFF` | 背景色 — 页面底色、卡片底色 |
| `#F5F5F5` | 次级背景 — 卡片分组底、代码块底、输入框填充 |
| `#E0E0E0` | 分割线 — 列表分隔、卡片边界 |
| `#757575` | 次要文字 — 辅助说明、时间戳、音标、词性标签 |
| `#B0B0B0` | 占位文字 — 搜索框提示、未激活图标 |

### 9.2.2 夜间模式

| 色值 | 用途 |
|---|---|
| `#E5E5E5` | 主色 — 标题、正文、图标 |
| `#6B9FFF` | 强调色 — 在暗背景上适当提亮，保持对比度 |
| `#121212` | 背景色 — 页面底色 |
| `#1E1E1E` | 次级背景 — 卡片底、浮层底 |
| `#2C2C2E` | 三级背景 — 输入框填充、选中态底 |
| `#3A3A3C` | 分割线 |
| `#8E8E93` | 次要文字 |
| `#545458` | 占位文字 |

### 9.2.3 护眼模式

| 色值 | 用途 |
|---|---|
| `#3D2E1E` | 主色 — 深棕，降低与背景的对比度 |
| `#5D7EA8` | 强调色 |
| `#F5F0E6` | 背景色 — 米黄色，类似纸质书页 |
| `#EAE4D8` | 次级背景 — 翻译卡片、分组区域、输入框填充 |
| `#D8CEBC` | 分割线 — 卡片边界、列表分隔 |

### 9.2.4 语义色

| 色值 | 用途 |
|---|---|
| `#34C759` | 成功 — 短语已收录、操作完成 |
| `#FF3B30` | 错误 — 解析失败、删除确认 |
| `#FF9500` | 警告 — 网络异常提示、解析中 |

交互状态不得在组件内重复硬编码主题色。整句选中背景使用语义色 `sentenceHighlight`：由当前主题页面背景色与次级背景色各按 50% 混合得到，确保弱于单词强调色。

## 9.3 字体系统

### 9.3.1 英文

| 用途 | 字体 | 说明 |
|---|---|---|
| 阅读正文 | Georgia / Charter | 衬线体，接近纸质书阅读感 |
| UI 界面 | SF Pro（iOS）/ Roboto（Android） | 无衬线，保持系统原生感 |
| 代码/音标 | SF Mono / Roboto Mono | 等宽字体 |

### 9.3.2 中文

| 用途 | 字体 | 说明 |
|---|---|---|
| 阅读正文 | 思源宋体 / Noto Serif CJK | 衬线体，与英文搭配 |
| UI 界面 | 苹方（iOS）/ 思源黑体（Android） | 无衬线 |

### 9.3.3 字号与行高

| 层级 | 逻辑字号（iOS pt / Android sp） | 行高 | 字重 |
|---|---|---|---|
| 文章大标题 | 24 | 1.4 | Bold |
| 章节标题 | 20 | 1.4 | SemiBold |
| 阅读正文 | 16-18（用户可调） | 1.6-1.8 | Regular |
| 翻译卡片单词 | 22 | 1.3 | Bold |
| 翻译卡片释义 | 14 | 1.5 | Regular |
| 音标/标签 | 12 | 1.3 | Regular |
| 列表标题 | 16 | 1.4 | Medium |
| 辅助说明 | 12 | 1.4 | Regular |

所有文本应跟随系统字体缩放设置；放大后允许内容换行或容器增高，不得截断核心释义和操作标签。

## 9.4 空间与组件

### 9.4.1 间距系统

基于 4 个逻辑单位的间距体系（iOS 使用 pt、Android 使用 dp）：

| 标记 | 值 | 用途 |
|---|---|---|
| xs | 4 | 图标与文字间距、标签内边距 |
| sm | 8 | 列表项内边距、卡片内容内边距 |
| md | 16 | 页面边距、卡片之间间距、段落间距 |
| lg | 24 | 模块之间间距、页面顶部/底部留白 |
| xl | 32 | 大段落分隔、页面首尾 |

### 9.4.2 组件规范

| 属性 | 规范 |
|---|---|
| 圆角 | **0** — 全直角，无圆角 |
| 阴影 | **无** — 不使用 box-shadow/elevation |
| 边框 | **1pt/dp 实线**，使用当前主题分割线颜色，仅用于层级必要区分处 |
| 卡片层级 | 通过当前主题页面背景与次级背景的色差区分，不依赖阴影 |
| 翻译卡片 | 使用当前主题次级背景色，不使用阴影；以顶部 1pt/dp 分割线和居中拖动把手表明边界与可拖动性 |
| 其他浮层（菜单等） | 使用当前主题背景色和 1pt/dp 实线边框表明边界 |

### 9.4.3 图标

- **风格**：线性图标，统一 1.5pt/dp 描边宽度
- **导航栏图标**：激活态填充强调色（`#3D7AED`），未激活态填充 `#B0B0B0`
- **操作图标**：保持描边风格，颜色跟随上下文
- **视觉尺寸**：导航栏 24×24、列表内 20×20、卡片内 16×16（iOS 使用 pt、Android 使用 dp）
- **触控区域**：图标视觉尺寸与触控区域分离，交互控件最小触控区域为 iOS 44×44pt、Android 48×48dp

## 9.5 空状态与引导

**首次启动不直接展示传统空状态页**。优先通过引导浮层/步骤教程帮助用户完成首次导入；同时为用户取消导入、文件选择失败或删除全部文档后的真实空列表提供极简兜底状态：

- 首次启动展示 3 步引导浮层（导入文档 → 点击单词 → 积累生词）
- 引导完成直接唤起文件选择器
- 若用户跳过引导或取消文件选择，返回文档列表并显示极简空状态
- 极简空状态仅包含文案「导入你的第一篇英文文档」和单一主按钮「导入文档」，不使用插图、功能宣传或多个并列入口
- 顶部「+」按钮在空状态下继续保留，与主按钮触发相同的文件导入流程

---

# 十、交互动画设计

## 10.1 动画设计原则

- **首帧反馈 < 100ms**：点击后选中态或控件按压态须在 100ms 内出现；转场可在反馈出现后继续完成
- **分级时长**：页面、卡片等结构转场使用 200-300ms；颜色、透明度和按压态等微反馈使用 100-150ms
- **缓动曲线采用 ease-out / spring**：物理感、自然收束
- **不打断阅读流**：非用户持续操控的过渡动画不得让正文失去可交互状态超过 1s
- **尊重系统“减少动态效果”设置**：开启后取消共享元素飞入、弹跳、弹性过冲和重复闪烁，统一替换为 ≤ 150ms 的淡入淡出或直接状态切换
- **反馈不依赖单一感官**：Haptic 遵循系统开关，所有触觉反馈必须同时具有可见状态变化，并提供适合屏幕阅读器播报的状态文本

## 10.2 页面转场

### 10.2.1 文档卡片 → 阅读页（共享元素转场）

| 属性 | 值 |
|---|---|
| 类型 | 共享元素过渡（Shared Element Transition） |
| 共享元素 | 文档卡片 → 阅读页顶部标题栏 |
| 时长 | 300ms |
| 缓动 | `cubic-bezier(0.4, 0.0, 0.2, 1.0)`（标准 deceleration） |
| 行为 | 卡片底色展开撑满屏幕，标题移动至顶部栏位置，正文在过渡完成后淡入 |

### 10.2.2 阅读页单词 → 翻译卡片

| 属性 | 值 |
|---|---|
| 类型 | 即时选中反馈 + 底部卡片转场 |
| 时长 | 200ms |
| 行为 | 单词原地立即高亮，同时底部卡片滑出；不复制或移动正文中的单词，避免高频查询动画干扰阅读 |

### 10.2.3 其他页面间转场

| 转场 | 动画 |
|---|---|
| 文档列表 ↔ 生词本 ↔ 短语本 ↔ 我的 | 图标填充状态与颜色过渡（详见 10.3），页面内容直接切换（无滑动） |
| 列表 → 详情页 | 两个平台均按下级页面处理，使用各平台原生前进/返回转场，并支持对应的系统返回手势 |
| 模态页面（设置、关于、确认流程等） | 两个平台均从底部上滑覆盖，200ms；关闭后返回原有页面层级 |

## 10.3 Tab 导航切换

| 属性 | 值 |
|---|---|
| 图标动画 | 不执行缩放或弹跳；仅从描边/未激活状态过渡为激活填充状态，时长 150ms |
| 颜色动画 | 图标从 `#B0B0B0` 过渡到 `#3D7AED`，时长 150ms |
| 页面切换 | 直接替换，无滑动或淡入淡出 |
| 交互条件 | 仅当点击不同 Tab 时触发；重复点击当前 Tab 不做动画 |

## 10.4 翻译卡片交互

### 10.4.1 卡片滑出

| 属性 | 值 |
|---|---|
| 类型 | 底部滑入 |
| 时长 | 200ms |
| 缓动 | Spring：damping=0.8, stiffness=200 |
| 行为 | 卡片从屏幕底部外平移进入，带轻微弹性过冲（overshoot 约 4pt/dp），然后回弹至最终位置 |
| 外观 | 使用当前主题次级背景色，顶部保留 1pt/dp 分割线，不使用阴影 |
| 拖动把手 | 顶部居中显示 32×3pt/dp 的短线把手，使用当前主题分割线颜色；把手及其周围区域提供完整最小触控范围 |
| 阅读进度条 | 卡片开始滑出时立即隐藏并禁用拖动，避免与卡片底部手势竞争 |

### 10.4.2 卡片内容呈现

| 属性 | 值 |
|---|---|
| 核心信息 | 单词、音标、词性标签和本地核心释义随卡片首帧直接出现，不执行逐项动画 |
| 短语列表 | 本地结果就绪后整体显示；若稍晚于核心释义返回，使用 ≤ 150ms fade，不逐条交错出现 |
| 在线增强内容 | 异步返回后在对应区域以 ≤ 150ms fade 显示，不影响现有内容阅读和操作 |
| 连续点词 | 卡片保持当前位置，核心内容使用 150ms crossfade 切换，不重新执行卡片滑出动画 |

### 10.4.3 卡片收起

| 触发方式 | 动画 |
|---|---|
| 点击卡片外的正文区域 | 若未点中其他单词，卡片向下平移滑出，200ms ease-in；若点中其他单词，则仅切换卡片内容 |
| 手势下滑 | 卡片跟随手指（跟手拖动），松手后若位移 > 30% 卡片高度则继续滑出，否则弹回 |
| 点击新单词 | 卡片内容以 150ms crossfade 切换为新单词内容，卡片不重新滑出 |

卡片完全滑出屏幕后，阅读进度条恢复显示和拖动；卡片取消收起并弹回时，进度条保持隐藏。

### 10.4.4 卡片展开/收起

| 属性 | 值 |
|---|---|
| 紧凑档 | 目标为可用安全区域高度的 40%；根据单词、音标、核心释义和首屏短语所需高度，在 35%-50% 之间自适应。若系统字体放大后仍无法完整容纳，优先保证单词与核心释义可见，其余内容在卡片内滚动 |
| 展开档 | 目标为可用安全区域高度的 65%；根据扩展内容高度自适应，最高不超过 90%，并避开顶部状态栏、摄像头开孔和底部系统手势区域 |
| 手势上滑 | 从顶部拖动把手区域开始时，卡片在当前计算出的紧凑档与展开档之间跟手拖动，松手后吸附至最近档位；内容区域的纵向手势优先用于滚动卡片内容 |
| 吸附动画 | Spring，时长 250ms |
| 展开内容 | 更多释义、例句、同义词区域在高度扩展后以 fade 依次呈现 |
| 尺寸变化 | 屏幕旋转、分屏尺寸变化或系统字体缩放改变后重新计算两个档位，并保持用户当前所处的紧凑/展开语义状态 |

## 10.5 单词选中反馈

### 10.5.1 单词选中态

| 属性 | 值 |
|---|---|
| 单词高亮 | 单词底出现 2pt/dp 的当前主题强调色下划线，同时填充当前主题强调色 12% 透明度的底色 |
| 动画 | 下划线从左到右生长，120ms ease-out；底色瞬间填充 |
| 句子高亮 | 选中单词所在的完整句子静态填充语义色 `sentenceHighlight`；该颜色由当前主题页面背景与次级背景各按 50% 混合，视觉强度须明显弱于单词选中态 |
| 状态生命周期 | 翻译卡片打开期间保持句子高亮；卡片完全收起时立即撤销。切换到其他句子中的单词时，旧句高亮立即撤销，新句高亮立即出现，不执行过渡动画 |

### 10.5.2 选中切换

点击新单词时，上一个单词的选中态撤消（下划线收缩消失 100ms），新单词选中态立即触发，总切换感知 < 150ms。若新旧单词不在同一句，句子背景按 10.5.1 的生命周期规则直接切换，不执行动画。

### 10.5.3 短语定位联动

当用户在翻译卡片中点击某条短语时，先确保正文中的对应片段位于翻译卡片上方的可视区域，再执行一次柔和高亮：

| 属性 | 值 |
|---|---|
| 定位条件 | 若短语已完整可见则不滚动；若被卡片遮挡或位于屏幕外，以最小滚动距离将其移动至卡片上方可视区域，并保留至少 16pt/dp 安全间距 |
| 定位动画 | 采用平台短距离平滑滚动，时长 150-200ms；不得把短语强制滚动到页面顶部 |
| 高亮动画 | 使用当前主题强调色 15% 透明度填充短语背景，并在 400ms 内单次淡出至透明，不重复闪烁 |
| 减少动态效果 | 关闭平滑滚动并直接定位；短语使用静态高亮保持 600ms 后直接撤销 |
| 效果 | 用户能够看到被指向的正文短语，同时保持原有阅读位置的空间上下文 |

## 10.6 按钮与控件反馈

| 控件类型 | 按压反馈 | Haptic |
|---|---|---|
| 导航按钮（返回、更多） | 亮度降低 12% (overlay `#000000@12%`)，松手恢复，150ms | 无 |
| 列表项/卡片 | 亮度降低 8%，150ms | 无 |
| 主要操作按钮（导入、确认、收录） | 按下时亮度降低 15%，松手后 100ms ease-out 恢复；不执行缩放或回弹 | 轻震动（Light） |
| 切换开关（Toggle） | 开关滑块平滑移动 150ms，背景色变化 200ms | 无 |
| 删除/危险操作按钮 | 亮度降低 15%，无缩放 | 按下时无 Haptic；完成二次确认并执行删除时使用 Medium |
| 搜索框 | 尺寸和位置保持不变；获得焦点时显示系统光标，边框或搜索图标切换为当前主题强调色，150ms；占位文字按平台默认行为消失，不使用浮动标签 | 无 |

## 10.7 手势交互动画

### 10.7.1 滑动操作

| 手势 | 动画 | Haptic |
|---|---|---|
| 列表项左滑（操作区） | 列表项跟随手指向左平移，露出页面对应的操作按钮并在固定距离吸附；松手未达到吸附阈值则弹回。滑动距离不触发删除，删除必须点击红色「删除」按钮并完成二次确认 | 无 |

### 10.7.2 长按菜单

| 属性 | 值 |
|---|---|
| 触发 | 长按单词 ≥ 500ms |
| 动画 | 单词上方弹出上下文菜单（复制/加入生词本/分享），菜单从 `scale(0.8, opacity:0)` → `scale(1.0, opacity:1)`，Spring 200ms |
| Haptic | 长按识别成功时中等震动（Medium） |

### 10.7.3 误触防护

点按与滚动冲突由 iOS / Android 系统手势识别器处理，并采用平台默认 touch slop、速度和时长阈值。滚动手势一旦成立即取消候选单词点击；不得使用固定 px 距离或过短的自定义点击时限，以免在不同屏幕密度和辅助触控设置下误判。

## 10.8 加载与进度

### 10.8.1 文档首次加载

| 属性 | 值 |
|---|---|
| 样式 | 居中显示文档标题 + 进度条 + 百分比数字 |
| 进度条 | 强调色 `#3D7AED`，从左到右填充，缓动 linear |
| 进入 | 标题从 0.6 透明度淡入（200ms），进度条跟随出现 |
| 完成 | 进度条填满后以 150ms 淡出，页面内容同时淡入 |

### 10.8.2 文档阅读加载（大文档分页）

| 场景 | 动画 |
|---|---|
| 滚动到未解析区域 | 立即显示静态骨架块，不执行闪烁或扫光；真实文本就绪后以 150ms crossfade 替换骨架 |
| 骨架块样式 | 使用当前主题次级背景色，宽度按段落行结构在 60%-90% 范围内变化；同一占位区域加载期间宽度保持稳定，避免视觉跳动 |
| 减少动态效果 | 真实文本就绪后直接替换骨架，不执行 crossfade |

### 10.8.3 网络请求（在线翻译增强）

| 属性 | 值 |
|---|---|
| 样式 | 翻译卡片内对应区域显示极细进度条（2pt/dp 高），不阻塞已加载内容 |
| 完成 | 进度条收缩消失，在线内容淡入 |
| 超时/失败 | 进度条消失并保留本地内容；在对应区域以次要文字静态显示「在线增强暂不可用，已显示本地释义」和「重试」文字按钮，不使用红色警报、Toast 或横幅 |
| 不提示场景 | 用户主动开启隐私模式、关闭在线增强，或请求前已确定设备离线时，仅展示本地内容，不显示失败信息 |
| 无障碍 | 状态文字可被屏幕阅读器读取；请求失败后进行一次礼貌级别的状态播报，不抢占当前阅读焦点 |

## 10.9 触觉反馈（Haptic）汇总

| 场景 | 反馈类型 | 强度 |
|---|---|---|
| 单词被选中 | Selection | 极轻 |
| 按钮点击（主操作） | Impact Light | 轻 |
| 短语收录成功 | Notification Success | 中 |
| 长按菜单触发 | Impact Medium | 中 |
| Tab 切换 | 无 Haptic | — |
| 文档删除确认并执行 | Impact Medium | 中 |
| 翻译卡片滑出到位 | 无 Haptic | — |
| 列表左滑操作区吸附到位 | 无 Haptic | — |
| 设置开关切换 | 无 Haptic | — |

## 10.10 无反馈场景（明确不做动画）

| 场景 | 理由 |
|---|---|
| 完成阅读文档 | 不打断用户，保持极简 |
| 生词本数量里程碑 | 不设计签到/成就系统 |
| 错误提示 | 在错误发生的对应区域静态展示，不使用弹窗或抖动；在线增强失败按 10.8.3 处理 |
| 空列表 | 不使用插图或动效；显示极简静态空状态和单一「导入文档」主按钮（见 9.5） |
| Toast/横幅 | 不使用，关键操作反馈通过 Haptic + 按钮短暂状态变化传达 |

---

# 十一、视觉与交互设计决策记录

| 设计决策 | 选定方案 | 理由 |
|---|---|---|
| 视觉语言 | 轻量极简 | 阅读型 App，内容优先 |
| 色板 | 石墨（Graphite） | 极度克制，黑白灰为主，单一强调色，不分散注意力 |
| 圆角 | 0px（全直角） | 最大程度接近印刷品质感 |
| 阴影 | 无 | 以间距和线条代替阴影区分层级 |
| 阅读字体 | 衬线体（Georgia + 思源宋体） | 模拟纸质书阅读体验 |
| UI 字体 | 无衬线（SF Pro + 苹方） | 保持系统原生感 |
| 图标 | 线性 1.5px | 极简轻盈 |
| 页面转场 | 文档→阅读使用共享元素过渡；单词→翻译卡片使用即时选中反馈 | 保留页面间空间连续性，同时避免高频点词动画干扰阅读 |
| 详情页转场 | 两个平台均使用原生层级导航转场 | 相同信息层级保持一致语义；底部上滑仅用于模态内容 |
| 翻译卡片 | 次级背景、顶部把手、滑出后核心内容直接呈现 | 保持直角无阴影的编辑感，同时明确浮层边界、拖动入口和查询效率 |
| 翻译卡片高度 | 基于安全区域和内容计算的自适应两档 | 保留紧凑/展开操作模型，同时适配小屏幕、长释义和系统字体放大 |
| 单词选中 | 强调色下划线和底色 + 极浅静态句子背景 | 区分单词与上下文层级，同时避免整句背景抢夺注意力 |
| 短语联动 | 必要时最小距离定位 + 单次柔和高亮 | 确保对应正文片段不被卡片遮挡，避免重复闪烁打断阅读 |
| 按钮反馈 | 亮度变化；关键主操作配合轻震动 | 反馈明确且不引入缩放回弹，保持界面安静稳定 |
| 搜索框焦点 | 固定尺寸 + 强调色焦点态 | 避免布局位移和表单式浮动标签，保持系统原生与阅读界面稳定 |
| 加载态 | 进度条 + 标题 | 给明确预期，优于不确定性等待 |
| 大文档分页加载 | 静态骨架 + 150ms 内容交叉淡化 | 保持段落布局稳定，避免闪烁和扫光干扰阅读 |
| 滚动模式 | 连续滚动 + 可选分页吸附 | 兼顾不同阅读习惯 |
| 列表左滑 | 统一露出操作区，不允许滑到底删除 | 保持跨页面手势语义一致，避免误删；危险操作必须点击按钮并二次确认 |
| 点词查询历史 | 当前版本不提供卡片左右滑动切换 | 避免与卡片展开、收起和内容滚动产生手势竞争；连续点词仍可直接切换内容 |
| 阅读进度条层级 | 翻译卡片打开时隐藏并禁用，完全收起后恢复 | 两者占用相同底部区域，互斥可避免视觉遮挡和拖动手势冲突 |
| Tab 切换 | 图标填充状态 + 颜色变化 | 在不引入弹跳的情况下明确当前位置，保持导航安静稳定 |
| 空状态 | 首次启动引导式导入 + 取消/清空后的极简空状态 | 优先推动首次导入，同时覆盖取消文件选择和删除全部文档后的真实路径 |
| 触觉反馈 | 仅用于单词选中、主要操作、收录成功、长按和删除确认 | 聚焦关键确认节点，避免高频操作震动疲劳；不使用 Heavy 强度 |
| 完成反馈 | 无 | 保持安静，不打断用户 |
| 夜间模式 | 深黑底 + 提亮强调色 | `#121212` 背景减少 OLED 发光，`#6B9FFF` 在暗背景保持可读 |
| 在线增强失败 | 保留本地结果 + 区域内弱提示和重试 | 说明内容来源与完整性，同时不使用 Toast、横幅或弹窗打断阅读 |

---

*文档版本 V1.0，基于设计方案 V1.0 编写。视觉风格与交互动画设计已通过用户决策确认，后续随需求变更持续更新。*
